# temporary data storage in memory
spring.datasource.url = jdbc:h2:mem:tempdb

#
spring.datasource.driverClassName=org.h2.Driver

# temporary data storage in my pc
#spring.datasource.url = jdbc:h2:file:{file path}

# default username is sa
spring.datasource.username=kushagra

#default password is empty
spring.datasource.password=root

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Enabling H2 Console
spring.h2.console.enabled=true

# Custom H2 Console URL http://localhost:8080/h2Console
spring.h2.console.path=/h2Console

#Turn Statistics on 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#
#spring.jpa.hibernate.ddl-auto=create